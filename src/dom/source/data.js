/**
 * getStyle with computed results.
 *
 * Authors:
 *   Allex Wang <allex.wxn@gmail.com> (http://iallex.com/)
 */

import domcache from 'dom-cache'

export default function data (...args) {
  return domcache(...args)
}
